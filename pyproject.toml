[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lifeforge"
version = "0.2.0"
description = "Evolutionary AI Agent Flight Simulator — discover zero-day failures in autonomous agents."
requires-python = ">=3.10"
dependencies = [
    "numpy",
]

[project.optional-dependencies]
llm = ["litellm>=1.40", "tenacity>=8.2"]
mcp = ["mcp>=1.0"]
viz = ["matplotlib>=3.8"]
all = ["litellm>=1.40", "tenacity>=8.2", "mcp>=1.0", "matplotlib>=3.8"]

[project.scripts]
lifeforge = "lifeforge.cli.main:main"

[tool.setuptools.packages.find]
include = ["lifeforge*"]